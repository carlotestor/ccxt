total_lines=1731

========== watchOrders (lines 430-480) ==========
 430|     /**
 431|      * @method
 432|      * @name krakenfutures#watchOrders
 433|      * @description watches information on multiple orders made by the user
 434|      * @see https://docs.kraken.com/exchange/api-reference/futures-websocket/open_orders
 435|      * @see https://docs.kraken.com/exchange/api-reference/futures-websocket/open_orders_verbose
 436|      * @param {string} symbol not used by krakenfutures watchOrders
 437|      * @param {int} [since] not used by krakenfutures watchOrders
 438|      * @param {int} [limit] not used by krakenfutures watchOrders
 439|      * @param {object} [params] extra parameters specific to the exchange API endpoint
 440|      * @param {boolean} [params.verbose] whether to subscribe to the open_orders_verbose feed
 441|      * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 442|      */
 443|     override async watchOrders (symbol: Str = undefined, since: Int = undefined, limit: Int = undefined, params = {}): Promise<Order[]> {
 444|         if (this.markets === undefined) {
 445|             await this.loadMarkets ();
 446|         }
 447|         let verbose = false;
 448|         [ verbose, params ] = this.handleOptionAndParams (params, 'watchOrders', 'verbose', false);
 449|         const name = (verbose) ? 'open_orders_verbose' : 'open_orders';
 450|         let messageHash = (verbose) ? 'orders:verbose' : 'orders';
 451|         if (symbol !== undefined) {
 452|             const market = this.market (symbol);
 453|             messageHash += ':' + market['symbol'];
 454|         }
 455|         const orders = await this.subscribePrivate (name, messageHash, params);
 456|         if (this.newUpdates) {
 457|             limit = orders.getLimit (symbol, limit);
 458|         }
 459|         return this.filterBySinceLimit (orders, since, limit, 'timestamp', true);
 460|     }
 461| 
 462|     /**
 463|      * @method
 464|      * @name krakenfutures#watchMyTrades
 465|      * @description watches information on multiple trades made by the user
 466|      * @see https://docs.kraken.com/exchange/api-reference/futures-websocket/fills
 467|      * @param {string} symbol unified market symbol of the market orders were made in
 468|      * @param {int} [since] the earliest time in ms to fetch orders for
 469|      * @param {int} [limit] the maximum number of order structures to retrieve
 470|      * @param {object} [params] extra parameters specific to the exchange API endpoint
 471|      * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 472|      */
 473|     override async watchMyTrades (symbol: Str = undefined, since: Int = undefined, limit: Int = undefined, params = {}): Promise<Trade[]> {
 474|         if (this.markets === undefined) {
 475|             await this.loadMarkets ();
 476|         }
 477|         const name = 'fills';
 478|         let messageHash = 'myTrades';
 479|         if (symbol !== undefined) {
 480|             const market = this.market (symbol);

========== handleOrder head (lines 740-800) ==========
 740|         //         "time": 1698159920097,
 741|         //         "last_update_time": 1699835622988,
 742|         //         "qty": 1.1,
 743|         //         "filled": 0,
 744|         //         "limit_price": 20000,
 745|         //         "stop_price": 0,
 746|         //         "type": 'limit',
 747|         //         "order_id": '0eaf02b0-855d-4451-a3b7-e2b3070c1fa4',
 748|         //         "direction": 0,
 749|         //         "reduce_only": false
 750|         //         },
 751|         //         "is_cancel": false,
 752|         //         "reason": 'edited_by_user'
 753|         //     }
 754|         //
 755|         let orders = this.orders;
 756|         if (orders === undefined) {
 757|             const limit = this.safeInteger (this.options, 'ordersLimit');
 758|             orders = new ArrayCacheBySymbolById (limit);
 759|             this.orders = orders;
 760|         }
 761|         const order = this.safeValue (message, 'order');
 762|         if (order !== undefined) {
 763|             const marketId = this.safeString (order, 'instrument');
 764|             const feed = this.safeString (message, 'feed');
 765|             const messageHash = (feed === 'open_orders_verbose') ? 'orders:verbose' : 'orders';
 766|             const symbol = this.safeSymbol (marketId);
 767|             const orderId = this.safeString (order, 'order_id');
 768|             const previousOrders = this.safeValue (orders.hashmap, symbol, {});
 769|             const previousOrder = this.safeValue (previousOrders, orderId);
 770|             const reason = this.safeString (message, 'reason');
 771|             if ((previousOrder === undefined) || (reason === 'edited_by_user')) {
 772|                 const parsed = this.parseWsOrder (order);
 773|                 orders.append (parsed);
 774|                 client.resolve (orders, messageHash);
 775|                 client.resolve (orders, messageHash + ':' + symbol);
 776|             } else {
 777|                 const trade = this.parseWsTrade (order);
 778|                 if (previousOrder['trades'] === undefined) {
 779|                     previousOrder['trades'] = [];
 780|                 }
 781|                 previousOrder['trades'].push (trade);
 782|                 previousOrder['lastTradeTimestamp'] = trade['timestamp'];
 783|                 let totalCost: Str = '0';
 784|                 let totalAmount: Str = '0';
 785|                 const trades = previousOrder['trades'];
 786|                 for (let i = 0; i < trades.length; i++) {
 787|                     const currentTrade = trades[i];
 788|                     totalCost = Precise.stringAdd (totalCost, this.numberToString (currentTrade['cost']));
 789|                     totalAmount = Precise.stringAdd (totalAmount, this.numberToString (currentTrade['amount']));
 790|                 }
 791|                 if (Precise.stringGt (totalAmount, '0')) {
 792|                     previousOrder['average'] = Precise.stringDiv (totalCost, totalAmount);
 793|                 }
 794|                 previousOrder['cost'] = totalCost;
 795|                 const filledString = this.numberToString (trade['amount']);
 796|                 const stringOrderFilled = this.safeString (previousOrder, 'filled', '0');
 797|                 const totalFilled = Precise.stringAdd (stringOrderFilled, filledString);
 798|                 previousOrder['filled'] = totalFilled;
 799|                 const prevAmountString = this.safeString (previousOrder, 'amount');
 800|                 const remaining = Precise.stringSub (prevAmountString, totalFilled);

========== handleOrder tail (lines 800-875) ==========
 800|                 const remaining = Precise.stringSub (prevAmountString, totalFilled);
 801|                 previousOrder['remaining'] = remaining;
 802|                 if (previousOrder['fee'] === undefined) {
 803|                     previousOrder['fee'] = {
 804|                         'rate': undefined,
 805|                         'cost': '0',
 806|                         'currency': this.numberToString (this.safeString (trade['fee'], 'currency')),
 807|                     };
 808|                 }
 809|                 if ((previousOrder['fee']['cost'] !== undefined) && (this.safeNumber (trade['fee'], 'cost') !== undefined)) {
 810|                     const stringOrderCost = this.numberToString (previousOrder['fee']['cost']);
 811|                     const stringTradeCost = this.numberToString (this.safeNumber (trade['fee'], 'cost'));
 812|                     previousOrder['fee']['cost'] = Precise.stringAdd (stringOrderCost, stringTradeCost);
 813|                 }
 814|                 // update the newUpdates count
 815|                 orders.append (this.safeOrder (previousOrder));
 816|                 client.resolve (orders, messageHash + ':' + symbol);
 817|                 client.resolve (orders, messageHash);
 818|             }
 819|         } else {
 820|             const isCancel = this.safeValue (message, 'is_cancel');
 821|             if (isCancel === true) {
 822|                 // Kraken documents is_cancel as "fully filled, cancelled, or
 823|                 // rejected". Derive unified status from `reason` instead of
 824|                 // mapping every removal to canceled. Preserve reason on info
 825|                 // so consumers can tell a user cancel from liquidation, etc.
 826|                 const reason = this.safeString (message, 'reason');
 827|                 let status = 'canceled';
 828|                 if (reason === 'full_fill') {
 829|                     status = 'closed';
 830|                 }
 831|                 // get order without symbol
 832|                 for (let i = 0; i < orders.length; i++) {
 833|                     const currentOrder = orders[i];
 834|                     if (currentOrder['id'] === message['order_id']) {
 835|                         const info = this.extend (this.safeDict (currentOrder, 'info', {}), {
 836|                             'reason': reason,
 837|                         });
 838|                         orders[i] = this.extend (currentOrder, {
 839|                             'status': status,
 840|                             'info': info,
 841|                         });
 842|                         client.resolve (orders, 'orders');
 843|                         client.resolve (orders, 'orders:' + currentOrder['symbol']);
 844|                         client.resolve (orders, 'orders:verbose');
 845|                         client.resolve (orders, 'orders:verbose:' + currentOrder['symbol']);
 846|                         break;
 847|                     }
 848|                 }
 849|             }
 850|         }
 851|         return message;
 852|     }
 853| 
 854|     handleOrderSnapshot (client: Client, message: any) {
 855|         //
 856|         // verbose
 857|         //
 858|         //    {
 859|         //        "feed": "open_orders_verbose_snapshot",
 860|         //        "account": "0f9c23b8-63e2-40e4-9592-6d5aa57c12ba",
 861|         //        "orders": [
 862|         //            {
 863|         //                "instrument": "PI_XBTUSD",
 864|         //                "time": 1567428848005,
 865|         //                "last_update_time": 1567428848005,
 866|         //                "qty": 100.0,
 867|         //                "filled": 0.0,
 868|         //                "limit_price": 8500.0,
 869|         //                "stop_price": 0.0,
 870|         //                "type": "limit",
 871|         //                "order_id": "566942c8-a3b5-4184-a451-622b09493129",
 872|         //                "direction": 0,
 873|         //                "reduce_only": false
 874|         //            },
 875|         //            ...

========== handleOrderSnapshot (lines 890-935) ==========
 890|         //                "filled": 0,
 891|         //                "limit_price": 34900,
 892|         //                "stop_price": 13789,
 893|         //                "type": "stop",
 894|         //                "order_id": "723ba95f-13b7-418b-8fcf-ab7ba6620555",
 895|         //                "direction": 1,
 896|         //                "reduce_only": false,
 897|         //                "triggerSignal": "last"
 898|         //            },
 899|         //            ...
 900|         //        ]
 901|         //    }
 902|         const orders = this.safeValue (message, 'orders', []);
 903|         const limit = this.safeInteger (this.options, 'ordersLimit');
 904|         this.orders = new ArrayCacheBySymbolById (limit);
 905|         const feed = this.safeString (message, 'feed');
 906|         const messageHash = (feed === 'open_orders_verbose_snapshot') ? 'orders:verbose' : 'orders';
 907|         const symbols: Dict = {};
 908|         const cachedOrders = this.orders;
 909|         for (let i = 0; i < orders.length; i++) {
 910|             const order = orders[i];
 911|             const parsed = this.parseWsOrder (order);
 912|             const symbol = parsed['symbol'];
 913|             if (symbol !== undefined) {
 914|                 symbols[symbol] = true;
 915|             }
 916|             cachedOrders.append (parsed);
 917|         }
 918|         const length = this.orders.length;
 919|         if (length > 0) {
 920|             client.resolve (this.orders, messageHash);
 921|             const keys = Object.keys (symbols);
 922|             for (let i = 0; i < keys.length; i++) {
 923|                 const symbol = keys[i];
 924|                 const symbolMessageHash = messageHash + ':' + symbol;
 925|                 client.resolve (this.orders, symbolMessageHash);
 926|             }
 927|         }
 928|     }
 929| 
 930|     override parseWsOrder (order: any, market: Market = undefined) {
 931|         //
 932|         // update
 933|         //
 934|         //    {
 935|         //        "feed": "open_orders_verbose",

========== grep: open_orders / verbose / 'orders' hashes ==========
 434|      * @see https://docs.kraken.com/exchange/api-reference/futures-websocket/open_orders
 435|      * @see https://docs.kraken.com/exchange/api-reference/futures-websocket/open_orders_verbose
 440|      * @param {boolean} [params.verbose] whether to subscribe to the open_orders_verbose feed
 447|         let verbose = false;
 448|         [ verbose, params ] = this.handleOptionAndParams (params, 'watchOrders', 'verbose', false);
 449|         const name = (verbose) ? 'open_orders_verbose' : 'open_orders';
 450|         let messageHash = (verbose) ? 'orders:verbose' : 'orders';
 686|     handleOrder (client: Client, message: any) {
 688|         //  update (verbose)
 691|         //        "feed": "open_orders_verbose",
 712|         //        "feed": "open_orders",
 730|         //        "feed": "open_orders",
 737|         //         "feed": 'open_orders',
 765|             const messageHash = (feed === 'open_orders_verbose') ? 'orders:verbose' : 'orders';
 842|                         client.resolve (orders, 'orders');
 844|                         client.resolve (orders, 'orders:verbose');
 845|                         client.resolve (orders, 'orders:verbose:' + currentOrder['symbol']);
 854|     handleOrderSnapshot (client: Client, message: any) {
 856|         // verbose
 859|         //        "feed": "open_orders_verbose_snapshot",
 882|         //        "feed": "open_orders_snapshot",
 902|         const orders = this.safeValue (message, 'orders', []);
 906|         const messageHash = (feed === 'open_orders_verbose_snapshot') ? 'orders:verbose' : 'orders';
 935|         //        "feed": "open_orders_verbose",
1681|                 'open_orders_verbose': this.handleOrder,
1682|                 'open_orders_verbose_snapshot': this.handleOrderSnapshot,
1685|                 'open_orders': this.handleOrder,
1686|                 'open_orders_snapshot': this.handleOrderSnapshot,

========== method signatures ==========
  72|     async authenticate (params = {}) {
 104|     override async watchOrderBookForSymbols (symbols: string[], limit: Int = undefined, params = {}): Promise<OrderBook> {
 118|     async subscribePublic (name: string, symbols: string[], params = {}) {
 155|     async subscribePrivate (name: string, messageHash: string, params = {}) {
 181|     override async watchTicker (symbol: string, params = {}): Promise<Ticker> {
 199|     override async watchTickers (symbols: Strings = undefined, params = {}): Promise<Tickers> {
 222|     override async watchBidsAsks (symbols: Strings = undefined, params = {}): Promise<Tickers> {
 258|     override async watchTradesForSymbols (symbols: Str[], since: Int = undefined, limit: Int = undefined, params = {}): Promise<Trade[]> {
 293|     override async watchPositions (symbols: Strings = undefined, since: Int = undefined, limit: Int = undefined, params = {}): Promise<Position[]> {
 310|     handlePositions (client: any, message: any) {
 443|     override async watchOrders (symbol: Str = undefined, since: Int = undefined, limit: Int = undefined, params = {}): Promise<Order[]> {
 473|     override async watchMyTrades (symbol: Str = undefined, since: Int = undefined, limit: Int = undefined, params = {}): Promise<Trade[]> {
 499|     override async watchBalance (params = {}): Promise<Balances> {
 516|     handleTrade (client: Client, message: any) {
 686|     handleOrder (client: Client, message: any) {
 854|     handleOrderSnapshot (client: Client, message: any) {
1011|     handleTicker (client: Client, message: any) {
1055|     handleBidAsk (client: Client, message: any) {
1165|     handleOrderBookSnapshot (client: Client, message: any) {
1232|     handleOrderBook (client: Client, message: any) {
1265|     handleBalance (client: Client, message: any) {
1489|     handleMyTrades (client: Client, message: any) {
1585|     async watchMultiHelper (unifiedName: string, channelName: string, symbols: any = undefined, subscriptionArgs: any = undefined, params = {}) {
1635|     handleErrorMessage (client: Client, message: any): Bool {
1663|     override handleMessage (client: any, message: any) {
1698|     handleAuthenticate (client: Client, message: any) {

========== static ws fixtures (cwd=/root/workspace/ccxt) ==========
binance.json
bingx.json
bitget.json
bitvavo.json
bybit.json
coinbase.json
cryptocom.json
deribit.json
gate.json
htx.json
hyperliquid.json
kraken.json
kucoin.json
kucoinfutures.json
okx.json
paradex.json
phemex.json
poloniex.json
toobit.json
upbit.json
whitebit.json
--- krakenfutures ws fixture:
ls: cannot access 'ts/src/test/static/ws/krakenfutures.json': No such file or directory


