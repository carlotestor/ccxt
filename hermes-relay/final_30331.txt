=== 1. tox.ini qa env (what check-python-syntax runs in CI) ===
[tox]
envlist = py{310,311,312,313,314}
skipsdist = True
skip_missing_interpreters = False
ignore_basepython_conflict = True

[testenv]
basepython = python3
sitepackages = True
setenv =
    PYTHONPATH = {toxinidir}:{toxinidir}
deps =
    pip
    setuptools
    wheel
    aiohttp
    cryptography
    requests
    coincurve
commands =
    pip install -e {toxinidir}/..
    python ccxt/test/tests_init.py --idTests
    python ccxt/test/tests_init.py --sync --idTests

[testenv:qa]
allowlist_externals = ruff
changedir = {toxinidir}
commands = ruff ./ccxt/
deps = {toxinidir}/..[qa]

[testenv:type]
allowlist_externals = mypy
changedir = {toxinidir}
commands = mypy ./ccxt/
deps = {toxinidir}/..[type]

=== 2. restore PR transpile.ts, transpile a batch, count E211 ===
patch restored (want 2): 2
alpaca: E211=3  odd_quote_comments=1
binance: E211=30  odd_quote_comments=8
bybit: E211=15  odd_quote_comments=6
okx: E211=5  odd_quote_comments=1
kraken: E211=1  odd_quote_comments=2
coinbase: E211=12  odd_quote_comments=4

=== 3. REVERSE DESYNC: does the PR still collapse prose inside strings? ===
--- alpaca: prose parentheticals in generated python (should keep space) ---
451:            raise ExchangeError(self.id + ' fetchTime() missing timestamp')
454:            raise ExchangeError(self.id + ' fetchTime() missing timestamp')
457:            raise ExchangeError(self.id + ' fetchTime() missing timestamp')
460:            raise ExchangeError(self.id + ' fetchTime() missing timestamp')
526:            raise ExchangeError(self.id + ' parseMarket() missing marketId')
668:            raise NotSupported(self.id + ' fetchTrades() does not support ' + method + ', marketPublicGetV1beta3CryptoLocTrades and marketPublicGetV1beta3CryptoLocLatestTrades are supported')
--- search generated python for collapsed prose: word( inside a quoted message ---
426:                    'Invalid symbol': BadSymbol,  # {"message":"Invalid symbol(s): BTC/USDdsda does not match ^[A-Z]+/[A-Z]+$"}

=== 4. does the PR fix the ORIGINAL #30286 case (kraken / Exchange proxy msg)? ===
3235:        raise ExchangeError(self.id + " withdraw() requires a 'key' parameter (withdrawal key name, up on your account)")

=== 5. git diff stat of PR vs master ===
 build/transpile.ts | 55 ++++++++++++++++++++++++++++++++++++++++++++++--------
 1 file changed, 47 insertions(+), 8 deletions(-)

BYTES:
2438 /tmp/final_30331.txt
