
===== FULL PATCH: skip-tests.json (modified +174 -21) =====
@@ -82,6 +82,9 @@
             "orderBook": {
                 "spread": "bid > ask issue https://github.com/ccxt/ccxt/actions/runs/14901691674/job/41855075107?pr=25907#step:9:389"
             },
+            "fetchTrades": {
+                "sideSequence": "todo check"
+            },
             "watchTrades": {
                 "timestampSort": "order is messed"
             },
@@ -202,6 +205,9 @@
             },
             "fetchTickers": {
                 "checkActiveSymbols": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -376,7 +382,10 @@
             },
             "watchTradesForSymbols.java": "flaky on Java: tight overlap with watchTrades trips exchange flood-detection / connection-close (engine watchMultiple race window only Java's ForkJoinPool exposes deterministically)",
             "watchOrderBookForSymbols.java": "same as watchTradesForSymbols.java",
-            "watchOHLCVForSymbols.java": "same as watchTradesForSymbols.java"
+            "watchOHLCVForSymbols.java": "same as watchTradesForSymbols.java",
+            "fetchTrades": {
+                "sideSequence": "todo check"
+            }
         }
     },
     "bithumb": {
@@ -410,6 +419,10 @@
                 "quoteVolume": "incorrect",
                 "compareOHLC": "todo"
             },
+            "watchTrades":{
+                "side": "not set https://app.travis-ci.com/github/ccxt/ccxt/builds/267900037#L4312",
+                "timestampSort": "messed order coming from exchange"
+            },
             "fetchTickers": {
                 "checkActiveSymbols": "todo"
             },
@@ -544,6 +557,9 @@
                 "compareToZero": "tmp",
                 "compareToNextItem": "tmp",
                 "spread": "tmp"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -567,6 +583,9 @@
             },
             "fetchTickers": {
                 "checkActiveSymbols": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -578,6 +597,11 @@
                 "taker": "is undefined",
                 "maker": "is undefined"
             },
+            "fetchTrades": {
+                "side": "side is undefined",
+                "requireBothSides": "same",
+                "sideSequence": "todo check"
+            },
             "fetchCurrencies": {
                 "deposit": "not provided",
                 "withdraw": "not provided",
@@ -638,8 +662,9 @@
                 "activeMajorCurrencies": "todo"
             },
             "orderBook": {
-                "spread": "bids[0][0] (3787971.0) should be < than asks[0][0] (3787971.0)",
-                "timestamp": "https://app.travis-ci.com/github/ccxt/ccxt/builds/269572350#L4820"
+                "bid":"bids[0][0] (3787971.0) should be < than asks[0][0] (3787971.0)",
+                "spread":"bids[0][0] (3787971.0) should be < than asks[0][0] (3787971.0)",
+                "timestamp":"https://app.travis-ci.com/github/ccxt/ccxt/builds/269572350#L4820"
             },
             "ticker": {
                 "spread": "broken bid-ask"
@@ -667,6 +692,9 @@
             "orderBook": {
                 "compareToZero": "https://app.travis-ci.com/github/ccxt/ccxt/builds/263287870#L2201",
                 "compareToNextItem": "sometimes bid equals to next bid"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -707,7 +735,10 @@
             },
             "watchTradesForSymbols.java": "flaky on Java: tight overlap with watchTrades trips bybit flood-detection / connection-close (engine watchMultiple race window only Java's ForkJoinPool exposes deterministically)",
             "watchOrderBookForSymbols.java": "same as watchTradesForSymbols.java",
-            "watchOHLCVForSymbols.java": "same as watchTradesForSymbols.java"
+            "watchOHLCVForSymbols.java": "same as watchTradesForSymbols.java",
+            "fetchTrades": {
+                "sideSequence": "todo check"
+            }
         }
     },
     "bybiteu": {
@@ -787,6 +818,9 @@
             "fetchCurrencies": "todo: doesnt return more than 1 coins",
             "orderBook": {
                 "timestamp": "https://app.travis-ci.com/github/ccxt/ccxt/builds/269572350#L4893"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -810,6 +844,10 @@
             },
             "watchTickers": {
                 "compareOHLC": "24h high/low can lag the latest price, so close occasionally falls outside the 24h range"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check",
+                "requireBothSides": "todo check"
             }
         }
     },
@@ -857,7 +895,8 @@
                 "currencyIdAndCode": "some currencies show in fetchMarkets but not in fetchCurrencies"
             },
             "fetchTrades": {
-                "side": "side is undefined"
+                "side":"side is undefined",
+                "requireBothSides": "same"
             },
             "orderBook": {
                 "timestamp": "https://app.travis-ci.com/github/ccxt/ccxt/builds/269572350#L5064"
@@ -879,6 +918,9 @@
             },
             "ticker": {
                 "compareQuoteVolumeBaseVolume": "quote scale isn't right"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -910,6 +952,9 @@
             },
             "orderBook": {
                 "timestamp": "https://app.travis-ci.com/github/ccxt/ccxt/builds/269572350#L5210"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -961,7 +1006,12 @@
                 "active": "is undefined"
             },
             "orderBook": {
-                "compareToNextItem": "ask should be less than next ask"
+                "compareToNextItem":"ask should be less than next ask"
+            },
+            "fetchTrades": {
+                "emptyResponse": "sometimes response is empty",
+                "compareToNextItem":"ask should be less than next ask",
+                "sideSequence":"ask should be less than next ask"
             }
         }
     },
@@ -998,6 +1048,9 @@
             "fetchAccounts": {
                 "type": "type is not provided",
                 "code": "not provided"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -1081,6 +1134,9 @@
                 "type": "todo",
                 "activeMajorCurrencies": "todo",
                 "activeCurrenciesQuota": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -1126,7 +1182,10 @@
                 "ask": "same",
                 "average": "same"
             },
-            "fetchTickers": "requires custom param"
+            "fetchTickers": "requires custom param",
+            "fetchTrades": {
+                "side": "todo fix"
+            }
         }
     },
     "derive": {
@@ -1144,7 +1203,11 @@
                 "type": "not provided",
                 "activeMajorCurrencies": "todo"
             },
-            "fetchOrderBook": "not implemented"
+            "fetchOrderBook": "not implemented",
+            "fetchTrades": {
+                "takerOrMaker": "todo fix",
+                "sideSequence": "todo check"
+            }
         }
     },
     "digifinex": {
@@ -1179,7 +1242,11 @@
                 "id": "undefined",
                 "notional": "undefined"
             },
-            "fetchBalance": "tmp skip"
+            "fetchBalance": "tmp skip",
+            "fetchTrades": {
+                "sideSequence": "todo check",
+                "requireBothSides": "todo check"
+            }
         }
     },
     "dydx": {
@@ -1320,6 +1387,9 @@
             },
             "fetchTickers": {
                 "checkActiveSymbols": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -1344,6 +1414,9 @@
             },
             "orderBook": {
                 "timestamp": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -1391,6 +1464,9 @@
             "fetchWithdrawals": {
                 "fee": "not provided for withdrawal",
                 "updated": "not provided"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -1420,10 +1496,20 @@
         }
     },
     "limitless": {
-        "preferredEventQuery": "bitcoin"
+        "preferredEventQuery": "bitcoin",
+        "skipMethods": {
+            "fetchTrades": {
+                "sideSequence": "todo check"
+            }
+        }
     },
     "myriad": {
-        "preferredEventQuery": "bitcoin"
+        "preferredEventQuery": "bitcoin",
+        "skipMethods": {
+            "fetchTrades": {
+                "sideSequence": "todo check"
+            }
+        }
     },
     "hyperliquid": {
         "skipMethods": {
@@ -1465,6 +1551,9 @@
             },
             "fetchTickers": {
                 "checkActiveSymbols": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -1488,6 +1577,10 @@
             "fetchTickers": {
                 "checkActiveSymbols": "todo"
             },
+            "fetchTrades": {
+                "sideSequence": "broken from engine",
+                "timestampSort": "todo"
+            },
             "orderBook": {
                 "symbol": "undefined, todo fix https://app.travis-ci.com/github/ccxt/ccxt/builds/269484317#L3670"
             },
@@ -1518,7 +1611,8 @@
                 "checkActiveSymbols": "todo"
             },
             "fetchTrades": {
-                "fees": "missing"
+                "fees": "missing",
+                "sideSequence": "todo check"
             }
         }
     },
@@ -1551,7 +1645,8 @@
                 "activeMajorCurrencies": "todo"
             },
             "fetchTrades": {
-                "side": "side is undefined"
+                "side": "side is undefined",
+                "sideSequence": "todo check"
             },
             "fetchTickers": "negative values",
             "orderBook": {
@@ -1569,7 +1664,10 @@
         ],
         "httpsProxy": "http://188.245.226.105:8911",
         "skipMethods": {
-            "fetchPositions": "demo/test account holds no open positions and the fetchPositions test asserts a non-empty array; run with a funded position to re-enable"
+            "fetchPositions": "demo/test account holds no open positions and the fetchPositions test asserts a non-empty array; run with a funded position to re-enable",
+            "fetchTrades": {
+                "sideSequence": "todo check"
+            }
         }
     },
     "kraken": {
@@ -1626,7 +1724,11 @@
             },
             "watchTradesForSymbols.java": "flaky on Java: tight overlap with watchTrades trips krakenfutures 'Already subscribed to feed' (engine watchMultiple race window only Java's ForkJoinPool exposes deterministically)",
             "watchOrderBookForSymbols.java": "same as watchTradesForSymbols.java",
-            "watchOHLCVForSymbols.java": "same as watchTradesForSymbols.java"
+            "watchOHLCVForSymbols.java": "same as watchTradesForSymbols.java",
+            "fetchTrades": {
+                "timestampSort": "todo check",
+                "sideSequence": "todo check"
+            }
         },
         "timeout": 120000
     },
@@ -1741,6 +1843,10 @@
                 "deposit": "not provided",
                 "activeMajorCurrencies": "todo"
             },
+            "fetchTrades": {
+                "requireBothSides":"sometimes only one side trades happen",
+                "sideSequence": "todo check"
+            },
             "ticker": {
                 "open": "negative values",
                 "high": "negative values",
@@ -1790,7 +1896,8 @@
                 "checkActiveSymbols": "todo"
             },
             "watchTrades": {
-                "timestamp": "ts several hours ahead in in future :)"
+                "timestamp": "ts several hours ahead in in future :)",
+                "timestampSort":"same"
             },
             "orderBook": {
                 "spread": "https://app.travis-ci.com/github/ccxt/ccxt/builds/269364330#L3612",
@@ -1815,6 +1922,9 @@
             "fetchCurrencies": {
                 "info": "key is missing",
                 "activeMajorCurrencies": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -1829,7 +1939,8 @@
                 "precision": "is undefined"
             },
             "fetchTrades": {
-                "side": "side is not buy/sell"
+                "side": "side is not buy/sell",
+                "sideSequence": "todo check"
             },
             "ticker": {
                 "compareQuoteVolumeBaseVolume": "https://app.travis-ci.com/github/ccxt/ccxt/builds/267900037#L6610",
@@ -1862,6 +1973,9 @@
                 "deposit": "not provided",
                 "activeMajorCurrencies": "todo",
                 "type": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -1888,6 +2002,9 @@
             "fetchCurrencies": {
                 "precision": "not provided",
                 "activeMajorCurrencies": "perp assets are included for market currency id validation but are not depositable or withdrawable"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -2124,6 +2241,9 @@
             },
             "fetchTickers": {
                 "checkActiveSymbols": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -2162,7 +2282,7 @@
                 "type": "todo"
             },
             "fetchTrades": {
-                "fees": "missingn from structure"
+                "fees": "missing from structure"
             },
             "fetchTickers": "temporary issues"
         }
@@ -2179,6 +2299,9 @@
                 "type": "tmp",
                 "limits": "tmp"
             },
+            "watchTrades": {
+                "timestampSort": "timestamp reversed: https://app.travis-ci.com/github/ccxt/ccxt/builds/269484317#L3697"
+            },
             "fetchLastPrices": "tmp",
             "ticker": {
                 "compareQuoteVolumeBaseVolume": "tmp"
@@ -2239,6 +2362,10 @@
             "watchTrades": {
                 "side": "not set"
             },
+            "fetchTrades": {
+                "fees": "missing from structure",
+                "side": "only sell sides are returned"
+            },
             "watchOrderBook": {
                 "spread": "cross"
             },
@@ -2254,6 +2381,10 @@
             "ticker": {
                 "compareQuoteVolumeBaseVolume": "quoteVolume >= baseVolume * low is failing"
             },
+            "fetchTrades": {
+                "side": "sometimes does not have",
+                "requireBothSides":"sometimes only one side trades happen"
+            },
             "fetchCurrencies": {
                 "activeMajorCurrencies": "todo"
             }
@@ -2273,6 +2404,10 @@
             },
             "fetchTickers": {
                 "checkActiveSymbols": "todo"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check",
+                "requireBothSides": "todo check"
             }
         }
     },
@@ -2316,7 +2451,10 @@
                 "active": "undefined",
                 "currencyIdAndCode": "messed"
             },
-            "fetchCurrencies": "doesnt return currencies, todo"
+            "fetchCurrencies": "doesnt return currencies, todo",
+            "fetchTrades": {
+                "sideSequence": "todo check"
+            }
         }
     },
     "wazirx": {
@@ -2336,7 +2474,8 @@
                 "limits": "incorrect for some currencies"
             },
             "fetchTrades": {
-                "side": "not set"
+                "side": "not set",
+                "sideSequence": "todo check"
             },
             "watchTickers": {
                 "compareOHLC": "broken by exchange https://github.com/ccxt/ccxt/actions/runs/16057416332/job/45315454452?pr=26334#step:11:2055"
@@ -2433,6 +2572,10 @@
             },
             "watchOrderBook": {
                 "spread": " bid > ask issue, https://github.com/ccxt/ccxt/actions/runs/19507537406/job/55838385322?pr=25884"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check",
+                "requireBothSides": "todo check"
             }
         }
     },
@@ -2467,6 +2610,15 @@
                 "precision": "not provided by the exchange api",
                 "networks": "not provided by the exchange api",
                 "activeMajorCurrencies": "deposit and withdraw flags are not provided by the exchange api"
+            },
+            "watchTrades": {
+                "takerOrMaker": "todo fix"
+            },
+            "watchTradesForSymbols": {
+                "takerOrMaker": "todo fix"
+            },
+            "fetchTrades": {
+                "sideSequence": "todo check"
             }
         }
     },
@@ -2520,7 +2672,8 @@
                 "timestamp": "not provided"
             },
             "fetchTrades": {
-                "side": "not provided"
+                "side": "not provided",
+                "sideSequence": "todo check"
             },
             "watchTrades": {
                 "side": "not provided"

===== FULL PATCH: ts/src/prediction/kalshi.ts (modified +1 -1) =====
@@ -1311,7 +1311,7 @@ export default class kalshi extends Exchange {
         const ticker = this.safeString (outcomeObj['info'], 'ticker');
         const request: Dict = { 'ticker': ticker };
         if (limit !== undefined) {
-            request['limit'] = limit;
+            request['limit'] = Math.min (limit, 1000);
         }
         const response = await this.kalshiPublicGetMarketsTrades (this.extend (request, params));
         const trades = this.safeList (response, 'trades', []);

===== FULL PATCH: ts/src/prediction/limitless.ts (modified +1 -1) =====
@@ -1231,7 +1231,7 @@ export default class limitless extends Exchange {
             'slug': slug,
         };
         if (limit !== undefined) {
-            request['limit'] = limit;
+            request['limit'] = Math.min (limit, 100);
         }
         const response = await this.limitlessPublicGetMarketsSlugEvents (this.extend (request, params));
         //

===== FULL PATCH: ts/src/pro/test/Exchange/test.watchMyTrades.ts (modified +1 -1) =====
@@ -24,7 +24,7 @@ async function testWatchMyTrades (exchange: Exchange, skippedProperties: object,
             testSharedMethods.assertNonEmtpyArray (exchange, skippedProperties, method, response, symbol);
             now = exchange.milliseconds ();
             for (let i = 0; i < response.length; i++) {
-                testTrade (exchange, skippedProperties, method, response[i], symbol, now);
+                testTrade (exchange, skippedProperties, method, response[i], symbol, now, false);
             }
             testSharedMethods.assertTimestampOrder (exchange, method, symbol, response);
         }

===== FULL PATCH: ts/src/pro/test/Exchange/test.watchTrades.ts (modified +1 -1) =====
@@ -26,7 +26,7 @@ async function testWatchTrades (exchange: Exchange, skippedProperties: object, s
         if (success === true) {
             testSharedMethods.assertNonEmtpyArray (exchange, skippedProperties, method, response);
             for (let i = 0; i < response.length; i++) {
-                testTrade (exchange, skippedProperties, method, response[i], symbol, now);
+                testTrade (exchange, skippedProperties, method, response[i], symbol, now, true);
             }
             if ((now - startTime) > maxIdleTime) {
                 idle = true;

===== FULL PATCH: ts/src/pro/test/Exchange/test.watchTradesForSymbols.ts (modified +8 -8) =====
@@ -2,16 +2,19 @@
 import assert from 'assert';
 import testTrade from '../../../test/Exchange/base/test.trade.js';
 import testSharedMethods from '../../../test/Exchange/base/test.sharedMethods.js';
-import { Exchange, Str, Trade } from '../../../../ccxt.js';
+import { Exchange, Trade } from '../../../../ccxt.js';
 
 async function testWatchTradesForSymbols (exchange: Exchange, skippedProperties: object, symbols: string[]) {
     const method = 'watchTradesForSymbols';
     let now = exchange.milliseconds ();
     const ends = now + 15000;
+    // hard ceiling on top of `ends`, so waiting for full symbol coverage cannot spin indefinitely
+    // when one symbol streams briskly while another stays quiet without ever tripping `idle`
+    const hardEnds = ends + 15000;
     const maxIdleTime = 5000;
     let idle = false;
     const returnedSymbols: string[] = [];
-    while ((now < ends) && !idle) {
+    while (((now < ends) || (returnedSymbols.length < symbols.length && now < hardEnds)) && !idle) {
         let response: Trade[] | undefined = undefined;
         let success = true;
         const startTime = exchange.milliseconds ();
@@ -26,14 +29,11 @@ async function testWatchTradesForSymbols (exchange: Exchange, skippedProperties:
         now = exchange.milliseconds ();
         if ((success === true) && (response !== undefined)) {
             assert (Array.isArray (response), exchange.id + ' ' + method + ' ' + exchange.json (symbols) + ' must return an array. ' + exchange.json (response));
-            let symbol: Str = undefined;
             for (let i = 0; i < response.length; i++) {
                 const trade = response[i];
-                symbol = trade['symbol'];
-                if (symbol === undefined) {
-                    continue;
-                }
-                testTrade (exchange, skippedProperties, method, trade, symbol, now);
+                const symbol = trade['symbol'];
+                assert (symbol !== undefined, exchange.id + ' ' + method + ' returned a trade without a symbol ' + exchange.json (trade));
+                testTrade (exchange, skippedProperties, method, trade, symbol, now, true);
                 testSharedMethods.assertInArray (exchange, skippedProperties, method, trade, 'symbol', symbols);
                 if (!exchange.inArray (symbol, returnedSymbols)) {
                     returnedSymbols.push (symbol);

===== FULL PATCH: ts/src/test/Exchange/base/test.order.ts (modified +1 -1) =====
@@ -53,7 +53,7 @@ function testOrder (exchange: Exchange, skippedProperties: object, method: strin
         const skippedNew = exchange.deepExtend (skippedProperties, { 'timestamp': true, 'datetime': true, 'side': true });
         if ((entry as Dict)['trades'] !== undefined) {
             for (let i = 0; i < (entry as Dict)['trades'].length; i++) {
-                testTrade (exchange, skippedNew, method, (entry as Dict)['trades'][i], symbol, now);
+                testTrade (exchange, skippedNew, method, (entry as Dict)['trades'][i], symbol, now, false);
             }
         }
     }

===== FULL PATCH: ts/src/test/Exchange/base/test.trade.ts (modified +8 -2) =====
@@ -2,7 +2,7 @@ import { Exchange } from "../../../../ccxt.js";
 import testSharedMethods from './test.sharedMethods.js';
 import type { Dict } from '../../../base/types.js';
 
-function testTrade (exchange: Exchange, skippedProperties: object, method: string, entry: object, symbol: string, now: number) {
+function testTrade (exchange: Exchange, skippedProperties: object, method: string, entry: object, symbol: string, now: number, isPublicTrade: boolean) {
     // prediction-market structures are keyed by an outcome handle, not a `symbol`, and the
     // PredictionTrade type carries a single `fee` but omits the `fees` list entirely
     if (exchange.safeBool (exchange.has, 'prediction', false)) {
@@ -34,7 +34,13 @@ function testTrade (exchange: Exchange, skippedProperties: object, method: strin
     testSharedMethods.assertSymbol (exchange, skippedProperties, method, entry, 'symbol', symbol);
     //
     testSharedMethods.assertInArray (exchange, skippedProperties, method, entry, 'side', [ 'buy', 'sell' ]);
-    testSharedMethods.assertInArray (exchange, skippedProperties, method, entry, 'takerOrMaker', [ 'taker', 'maker' ]);
+    if (isPublicTrade) {
+        // for public trades (fetchTrades & watchTrades), it must be either 'taker' or undefined
+        testSharedMethods.assertInArray (exchange, skippedProperties, method, entry, 'takerOrMaker', [ 'taker', undefined ]);
+    } else {
+        // for private trades (fetchMyTrades & watchMyTrades), it can be any
+        testSharedMethods.assertInArray (exchange, skippedProperties, method, entry, 'takerOrMaker', [ 'taker', 'maker', undefined ]);
+    }
     testSharedMethods.assertFeeStructure (exchange, skippedProperties, method, entry, 'fee');
     if (!('fees' in skippedProperties)) {
         // todo: remove undefined check and probably non-empty array check later

===== FULL PATCH: ts/src/test/Exchange/test.fetchMyTrades.ts (modified +1 -1) =====
@@ -8,7 +8,7 @@ async function testFetchMyTrades (exchange: Exchange, skippedProperties: object,
     testSharedMethods.assertNonEmtpyArray (exchange, skippedProperties, method, trades, symbol);
     const now = exchange.milliseconds ();
     for (let i = 0; i < trades.length; i++) {
-        testTrade (exchange, skippedProperties, method, trades[i], symbol, now);
+        testTrade (exchange, skippedProperties, method, trades[i], symbol, now, false);
     }
     testSharedMethods.assertTimestampOrder (exchange, method, symbol, trades);
     return true;

===== FULL PATCH: ts/src/test/Exchange/test.fetchTrades.ts (modified +88 -5) =====
@@ -1,19 +1,102 @@
+import assert from 'assert';
 import { Exchange } from "../../../ccxt.js";
 import testSharedMethods from './base/test.sharedMethods.js';
 import testTrade from './base/test.trade.js';
+import Precise from '../../base/Precise.js';
+
 
 async function testFetchTrades (exchange: Exchange, skippedProperties: object, symbol: string) {
     const method = 'fetchTrades';
-    const trades = await exchange.fetchTrades (symbol);
+    const trades = await exchange.fetchTrades (symbol, undefined, 12000); // lets test with unrealistically high amount
+    return await testFetchTradesResponseHelper (exchange, skippedProperties, symbol, method, trades);
+}
+
+async function testFetchTradesResponseHelper (exchange: Exchange, skippedProperties: object, symbol: string, method: string, trades: any[]) {
     testSharedMethods.assertNonEmtpyArray (exchange, skippedProperties, method, trades);
-    const now = exchange.milliseconds ();
-    for (let i = 0; i < trades.length; i++) {
-        testTrade (exchange, skippedProperties, method, trades[i], symbol, now);
-        testSharedMethods.assertInArray (exchange, skippedProperties, method, trades[i], 'takerOrMaker', [ 'taker', undefined ]);
+    await testFetchTradesStructureHelper (exchange, skippedProperties, symbol, method, trades);
+    // a market that legitimately traded only one side within the fetched window is common in
+    // thin markets, so this needs a sample large enough that a real "only one side" bug (rather
+    // than a quiet market) is the more likely explanation
+    const minTradesForBothSidesCheck = 300;
+    if (!('requireBothSides' in skippedProperties) && trades.length > minTradesForBothSidesCheck) {
+        await testFetchTradesSidesHelper (exchange, skippedProperties, symbol, method, trades);
     }
     if (!('timestampSort' in skippedProperties)) {
         testSharedMethods.assertTimestampOrder (exchange, method, symbol, trades);
     }
+    if (!('sideSequence' in skippedProperties)) {
+        await testFetchTradesSideSequenceHelper (exchange, skippedProperties, symbol, method, trades);
+    }
+    return true;
+}
+
+async function testFetchTradesStructureHelper (exchange: Exchange, skippedProperties: object, symbol: string, method: string, trades: any[]) {
+    const now = exchange.milliseconds ();
+    const isPublicTrade = true;
+    for (let i = 0; i < trades.length; i++) {
+        testTrade (exchange, skippedProperties, method, trades[i], symbol, now, isPublicTrade);
+    }
+}
+
+async function testFetchTradesSidesHelper (exchange: Exchange, skippedProperties: object, symbol: string, method: string, trades: any[]) {
+    //
+    //    Check whether both "buy" and "sell" are returned from trades, when there are enough trades
+    //  for a one-sided result to be an implausible coincidence (see minTradesForBothSidesCheck)
+    //
+    const grouped = exchange.groupBy (trades, 'side');
+    const msg = 'Both sides of trades are not being returned, instead only one side is being returned. If this error happens consistently, then it might be an implementation issue' + testSharedMethods.logTemplate (exchange, method, trades);
+    assert (('buy' in grouped), msg);
+    assert (('sell' in grouped), msg);
+}
+
+async function testFetchTradesSideSequenceHelper (exchange: Exchange, skippedProperties: object, symbol: string, method: string, trades: any[]) {
+    //
+    //     Check whether side is correct. This can be found out deterministically,
+    //   by checking an order that has been filled with multiple trades at the
+    //   same time (but on different prices). The price between first and last
+    //   trade will definitely be directional. for example, an order with 3 fills:
+    //       - 1600000000073 : ...
+    //       - 1600000000111 : 0.4 ETH at 1750.40
+    //       - 1600000000111 : 0.9 ETH at 1750.41
+    //       - 1600000000111 : 0.33 ETH at 1750.42
+    //       - 1600000000252 : ...
+    //     Here it's definitely visible that the trades have been `buy` as it happened
+    //   on same timestamp and trades are increasing in price. if it was `sell` the
+    //   prices would have been in decreasing order. it's nearly impossible to happen
+    //   otherwie (if such rare event happens ever, the test can be restarted and
+    //   the new run would not meet such exceptional case)
+    //
+    //     the heuristic only holds for ascending-by-timestamp input, which is not guaranteed by
+    //   every exchange (see the `timestampSort` skip), so scan a locally-sorted copy instead of
+    //   relying on the order `trades` was actually returned in
+    //
+    const sortedTrades = exchange.sortBy (trades, 'timestamp');
+    let lastTs = undefined;
+    let lastPrice = undefined;
+    let lastSide = undefined;
+    for (let i = 0; i < sortedTrades.length; i++) {
+        const trade = sortedTrades[i];
+        const ts = trade['timestamp'];
+        const price = exchange.safeString (trade, 'price');
+        const side = trade['side'];
+        //
+        const isSameTs = ts === lastTs;
+        const isSamePrice = Precise.stringEq (price, lastPrice);
+        const isSameSide = side === lastSide;
+        // we are only interested in trades that have: same timestamp, same side, but different(!) price
+        if (isSameTs && isSameSide && !isSamePrice) {
+            const priceIncreasing = Precise.stringGt (price, lastPrice);
+            const priceDecreasing = Precise.stringLt (price, lastPrice);
+            if (priceIncreasing) {
+                assert (side === 'buy', 'Side should be `buy` if price is increasing' + testSharedMethods.logTemplate (exchange, method, trade));
+            } else if (priceDecreasing) {
+                assert (side === 'sell', 'Side should be `sell` if price is decreasing' + testSharedMethods.logTemplate (exchange, method, trade));
+            }
+        }
+        lastPrice = price;
+        lastTs = ts;
+        lastSide = side;
+    }
     return true;
 }
 