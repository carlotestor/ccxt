=== PERF: old vs new E225 rule on real ts/src bodies ===
file | bytes | E225 matches | old ms | new ms | slowdown
base/Exchange.ts | 491835 | 4413 | 3.6 | 1719.9 | 471x
binance.ts | 826101 | 4042 | 5.2 | 3710.5 | 709x
okx.ts | 478950 | 2317 | 2.9 | 1139.4 | 399x
kraken.ts | 171551 | 1049 | 1.1 | 198.5 | 180x
bybit.ts | 518313 | 2401 | 2.8 | 1324.5 | 476x
hyperliquid.ts | 226128 | 1481 | 1.2 | 339.2 | 276x
TOTAL old=16.9ms new=8431.9ms ratio=500x

ts/src/*.ts files=104 totalBytes=15502407

=== CORRECTNESS: apostrophes in // comments (scanner has no comment awareness) ===
line-comments with an ODD number of quotes: 143
  alpaca.ts:1951: // TRANS entries may carry symbol/asset - never blindly adopt the caller's
  binance.ts:3537: //           id: "376870555451677893", // doesn't exist in isolated
  binance.ts:3906: // therefore limits['price']['max'] doesn't have any meaningful value except undefined
  binance.ts:5110: // the reality is that the time range wider than 500 candles won't work right
  binance.ts:5139: // It didn't work before without the endTime
  binance.ts:5870: // remove timeInForce from params because PO is only used by this.isPostOnly and it's not a valid va
  binance.ts:6954: // don't handle/omit params here, omitting happens inside createOrderRequest
  binance.ts:7382: // remove timeInForce from params because PO is only used by this.isPostOnly and it's not a valid va
  binance.ts:8889: // If startTime and endTime are both not sent, then the last 7 days' data will be returned.
  binance.ts:11924: // if it is a dict, then it doesn't seem to have any 'method', so set default value
  binance.ts:13014: // check if url matches the field's value
  bingx.ts:1550: // 'volume' is the exchange's own base-currency fill quantity (bingx linear contractSize is always 1

=== DEMO: desync from a prose comment ===
--- input ---
        // don't collapse this
        const x = this.safeString (a, 'b');
        throw new BadRequest (this.id + ' bad parameter (foo) here');
        this.doThing (x);
--- OLD ---
        // don't collapse this
        const x = this.safeString(a, 'b');
        throw new BadRequest(this.id + ' bad parameter(foo) here');
        this.doThing(x);
--- NEW ---
        // don't collapse this
        const x = this.safeString (a, 'b');
        throw new BadRequest (this.id + ' bad parameter(foo) here');
        this.doThing(x);
--- NEW without the comment (control) ---
        const x = this.safeString(a, 'b');
        throw new BadRequest(this.id + ' bad parameter (foo) here');
        this.doThing(x);
rc=0
