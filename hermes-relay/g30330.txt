
===== PR VIEW =====
{"additions":1,"author":{"id":"MDQ6VXNlcjI2Mzk4OTc4","is_bot":false,"login":"riba2534","name":"riba2534"},"body":"Fixes #30200.\n\nThe cast-strip regex in `getTypescriptRemovalRegexes` runs over whole-file text, so English ` as <word>` was removed from comments and thrown messages in the Python/PHP trees (hitbtc `convertCurrencyNetwork`, bitbank `70004`, kraken withdraw key text, bigone comment, etc.).\n\nTighten the second regex so the cast target must be a capitalized type or a TypeScript primitive with a word boundary (`\\b` — without it `as strings` becomes `stringss`). Real casts like `as any` / `as number` still strip.\n\nComplementary to #29822 (comment masking): that PR protects comments but not string literals; this one covers both by fixing the regex itself.\n\nVerified with:\n```\nnpx tsx build/transpile.ts hitbtc bitbank kraken bigone --python --force\nnpx tsx build/transpile.ts hitbtc bitbank kraken bigone --php --force\n```\nand `python3 -m py_compile` on the regenerated exchange files. Only `build/transpile.ts` is committed; CI regenerates the rest.","deletions":1,"files":[{"path":"build/transpile.ts","additions":1,"deletions":1}],"headRefOid":"8afa118521cf9a9f7060577a70a6887b8a14cea5","isDraft":false,"labels":[],"state":"OPEN","title":"fix(build): keep English \"as <word>\" in comments and thrown messages"}

===== CLASSIFY GENERATED =====
{
  "generated": [],
  "source": [
    {
      "path": "build/transpile.ts",
      "why": "source-of-truth / not a generated dump"
    }
  ],
  "unknown": [],
  "close": false,
  "reason": "no generated files",
  "generated_paths": [],
  "source_paths": [
    "build/transpile.ts"
  ]
}

===== PRIOR CARLOTESTOR REVIEWS =====
0

===== PRIOR CARLOTESTOR ISSUE COMMENTS =====
0

===== FULL DIFF =====
diff --git a/build/transpile.ts b/build/transpile.ts
index 731e0d7eb61b4..632e215bd8b07 100644
--- a/build/transpile.ts
+++ b/build/transpile.ts
@@ -806,7 +806,7 @@ class Transpiler {
     getTypescriptRemovalRegexes() {
         return [
             [ /(?<![a-zA-Z0-9_]\s)(?<![a-zA-Z0-9_])\((\w+)\sas\s\w+\)/g, '$1'], // remove parens around a cast like "(x as any)" -> "x"; but NOT when it's a call arg, in either the spaced "foo (x as string)" or unspaced "foo(x as string)" form (the latter is produced by trimmedUnCamelCase collapsing base-method calls, e.g. capitalize(side as string)) — both keep their parens and let the next rule drop just the " as T"
-            [ /\sas (\w+<[^<>]*(?:<[^<>]*>[^<>]*)*>|(?:Dictionary<)?\w+(?:\[])?>?)/g, ''], // remove any "as any" or "as number" or "as trade[]" or a generic cast such as "as Endpoint<Dict | List>" (the generic arm must run first, otherwise "as Foo" matches and strands "<T>")
+            [ /\sas (\w+<[^<>]*(?:<[^<>]*>[^<>]*)*>|(?:Dictionary<)?(?:[A-Z]\w*|(?:any|number|string|boolean|bigint|unknown|object|never|void|symbol)\b)(?:\[])?>?)/g, ''], // remove any "as any" or "as number" or "as trade[]" or a generic cast such as "as Endpoint<Dict | List>" (the generic arm must run first, otherwise "as Foo" matches and strands "<T>")
             [ /(^|[^a-zA-Z0-9_])((?:let|const)\s+\w+):[^=\n]+(\s+=.*$)/gm, '$1$2$3'], // remove variable type
         ]
     }

===== FILES PATCH WITH HUNK HEADERS =====
{"additions":1,"deletions":1,"filename":"build/transpile.ts","patch":"@@ -806,7 +806,7 @@ class Transpiler {\n     getTypescriptRemovalRegexes() {\n         return [\n             [ /(?\u003c![a-zA-Z0-9_]\\s)(?\u003c![a-zA-Z0-9_])\\((\\w+)\\sas\\s\\w+\\)/g, '$1'], // remove parens around a cast like \"(x as any)\" -\u003e \"x\"; but NOT when it's a call arg, in either the spaced \"foo (x as string)\" or unspaced \"foo(x as string)\" form (the latter is produced by trimmedUnCamelCase collapsing base-method calls, e.g. capitalize(side as string)) — both keep their parens and let the next rule drop just the \" as T\"\n-            [ /\\sas (\\w+\u003c[^\u003c\u003e]*(?:\u003c[^\u003c\u003e]*\u003e[^\u003c\u003e]*)*\u003e|(?:Dictionary\u003c)?\\w+(?:\\[])?\u003e?)/g, ''], // remove any \"as any\" or \"as number\" or \"as trade[]\" or a generic cast such as \"as Endpoint\u003cDict | List\u003e\" (the generic arm must run first, otherwise \"as Foo\" matches and strands \"\u003cT\u003e\")\n+            [ /\\sas (\\w+\u003c[^\u003c\u003e]*(?:\u003c[^\u003c\u003e]*\u003e[^\u003c\u003e]*)*\u003e|(?:Dictionary\u003c)?(?:[A-Z]\\w*|(?:any|number|string|boolean|bigint|unknown|object|never|void|symbol)\\b)(?:\\[])?\u003e?)/g, ''], // remove any \"as any\" or \"as number\" or \"as trade[]\" or a generic cast such as \"as Endpoint\u003cDict | List\u003e\" (the generic arm must run first, otherwise \"as Foo\" matches and strands \"\u003cT\u003e\")\n             [ /(^|[^a-zA-Z0-9_])((?:let|const)\\s+\\w+):[^=\\n]+(\\s+=.*$)/gm, '$1$2$3'], // remove variable type\n         ]\n     }"}

===== ISSUE 30200 =====
{"body":"`build/transpile.ts` removes ` as` and the word after it, to strip a typescript cast:\n\n```ts\n[ /\\sas (\\w+<[^<>]*(?:<[^<>]*>[^<>]*)*>|(?:Dictionary<)?\\w+(?:\\[])?>?)/g, ''],\n```\n\nThe substitution runs over the whole file text, and English uses those two words too, so it takes them out of comments and out of thrown messages. The result is valid code and nothing complains:\n\n    bigone.ts   // the actual response format is not the same as the documented one\n    bigone.py   # the actual response format is not the same documented one\n\n    hitbtc.ts   ' convertCurrencyNetwork() fromNetwork cannot be the same as toNetwork'\n    hitbtc.py   ' convertCurrencyNetwork() fromNetwork cannot be the same'\n\n    bitbank.ts  '70004': 'We are unable to accept orders as the transaction is currently suspended'\n    bitbank.py  '70004': 'We are unable to accept orders transaction is currently suspended'\n\n    kraken.ts   \" withdraw() requires a 'key' parameter (withdrawal key name, as set up on your account)\"\n    kraken.py   \" withdraw() requires a 'key' parameter(withdrawal key name, up on your account)\"\n\nhitbtc's is the one that matters most: that message exists to say which two things must differ, and in python it says only that they cannot be the same.\n\nOver `ts/src`, `ts/src/pro`, `ts/src/prediction` and the base class this affects **303 places in a line comment across 64 files** and **11 string literals across 9 files**, four of the literals being thrown messages that a user of the python, php, c-sharp, go or java package reads. Jsdoc is unaffected, since the transpiler drops it rather than translating it; I counted the 189 `ordered as timestamp` occurrences as damage first and then checked the generated files, where neither the mangled form nor the correct one appears.\n\nA one-line tightening would fix it: require the cast target to be a typescript primitive or a capitalised type. That is not a guess about ccxt's conventions, it is the source counted. Of 11976 ` as ` occurrences on code lines, 11064 are followed by a capitalised type and 882 by a primitive. The remaining 30 are not casts: 16 are import aliases, `import { keccak_256 as keccak }` and `sign as starknetCurveSign` and `* as functions`, and import lines are rewritten per language anyway; the rest is prose inside a comment or a thrown message.\n\n```ts\n[ /\\sas (\\w+<[^<>]*(?:<[^<>]*>[^<>]*)*>|(?:Dictionary<)?(?:[A-Z]\\w*|any|number|string|boolean|bigint|unknown|object|never|void|symbol)(?:\\[])?>?)/g, ''],\n```\n\nTranspiling the whole repository with that change rather than a sample: no transpiler error, 291 of 291 generated python files compile, the tree moves 1504 lines across 350 files with 1504 removed, bigone's comment reads correctly in python, php, go, c-sharp and java, `tsc --noEmit` clean, and the static response and base suites unchanged at 1677 and green. The 1504 does not contradict the 314 sites: each site is one swap in each of five generated languages, and several files exist twice under `async_support` and `pro`.\n\nI am raising this as an issue rather than a pull request because `CONTRIBUTING.md` lists `/build/*` among the files not to commit in a pull request. That line is aimed at the generated part of the directory, and the same document describes `/build/transpile.ts` as \"the transpilation script\", so it reads as imprecise rather than a prohibition. Still, it is your build and the call is yours. Say the word and I will open the one-line pull request; the diff and the census above are ready.\n\nIn the meantime there is a rule for anyone writing a ccxt comment: do not put `as` before another word. A colon works and becomes `=>` in php, which is only odd to read.\n","labels":[{"id":"MDU6TGFiZWw2MDQ4ODY5NjQ=","name":"bug","description":"","color":"D93A0F"}],"state":"OPEN","title":"build/transpile.ts removes \" as <word>\" from comments and thrown messages in five languages"}

===== PR 29822 STATE =====
{"author":{"id":"MDQ6VXNlcjEyOTQ0NTQ=","is_bot":false,"login":"kroitor","name":"Igor Kroitor"},"isDraft":false,"state":"OPEN","title":"fix(build): mask comment bodies from the code transforms in the transpiler"}

===== DONE =====
9098 /tmp/g30330.txt
