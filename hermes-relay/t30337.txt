=== deploy-playground.yml TRIGGER BLOCK (first 40 lines) ===
name: Deploy Playground
run-name: ${{ github.event.head_commit.message || github.event.pull_request.title || format('{0} · {1}', github.ref_name, github.sha) }}

# Builds the CCXT Playground as a container (amd64, matching the docs box) and
# deploys it as a long-running Next.js server behind nginx at /playground on
# docs.ccxt.com. Mirrors the Fumadocs flow: build image -> push to GHCR ->
# box pulls + canary smoke test -> promote (no downtime, auto-abort on failure).
#
# Reuses the same DOCS_DEPLOY_* secrets and box as docs-fumadocs.yml.
# One-time box setup (root): put the OpenRouter key in /root/ccxt-playground.env
#   OPENROUTER_API_KEY=sk-or-...
# and add an nginx `location /playground { proxy_pass http://172.31.0.10:3000; }`
# (the app's pinned IP on the internal network — see APP_IP below; publishing a
# host port doesn't work on a Docker `internal` network).

on:
  push:
    branches: [master]
    paths:
      - 'docs/playground/**'
      - '.github/workflows/deploy-playground.yml'
  workflow_dispatch:

concurrency:
  group: deploy-playground
  cancel-in-progress: false

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    if: github.repository == 'ccxt/ccxt'
    runs-on: [ self-hosted, linux, x64, deploy ]          # amd64 — native build matching the docs server
    env:
      IMAGE: ghcr.io/ccxt/ccxt-playground
      IMAGE_PROXY: ghcr.io/ccxt/ccxt-playground-egress
      CONTAINER: ccxt-playground
      PROXY: egress-proxy             # egress proxy container name (app reaches it by this DNS name)

=== Dockerfile lines 60-80 (npm ci context) ===
WORKDIR /app

# Create the unprivileged user BEFORE the heavy steps and build as that user,
# so every file is owned correctly from the start. The previous layout ran the
# 10.5 GB build/warm layer as root and then `chown -R`ed /app in a separate
# RUN — overlayfs copy-up duplicated the entire tree into a second ~11 GB
# layer, doubling the image.
RUN useradd -m -u 1001 playground && chown playground:playground /app
USER playground

# Node deps first (better layer caching). Use `npm ci` (lockfile-exact, fails on
# drift) rather than `npm install` — reproducible builds.
COPY --chown=playground:playground package.json package-lock.json ./
RUN npm ci

# --- Language runtimes (~10 GB, ~18 min) -------------------------------------
# Deliberately BEFORE the app source: this layer depends only on the script, the
# resolved Java stage and the version pins, so an app-source edit reuses it from
# cache. Bundling it with `npm run build` after `COPY . .` (as this did) meant a
# one-line source change re-provisioned every runtime and produced a brand-new
# ~10 GB layer to push and pull — ~29 min of deploy for a one-line edit.

=== FINAL PR CHECK STATE ===
windows	skipping	0	https://github.com/ccxt/ccxt/actions/runs/34314725028/job/102348709040	
build	pending	0	https://github.com/ccxt/ccxt/actions/runs/34314725030/job/102348442851	
build	pass	1m16s	https://github.com/ccxt/ccxt/actions/runs/34314724996/job/102348442912	
build	pass	1m36s	https://github.com/ccxt/ccxt/actions/runs/34314725041/job/102348442996	
live-tests	pass	48s	https://github.com/ccxt/ccxt/actions/runs/34314724966/job/102348767058	
live-tests	pass	1m0s	https://github.com/ccxt/ccxt/actions/runs/34314725028/job/102348708010	
live-tests	pass	31s	https://github.com/ccxt/ccxt/actions/runs/34314725039/job/102348633795	
live-tests	pass	2m42s	https://github.com/ccxt/ccxt/actions/runs/34314725041/job/102349245120	
live-tests	pass	48s	https://github.com/ccxt/ccxt/actions/runs/34314724996/job/102349202930	
build	pass	49s	https://github.com/ccxt/ccxt/actions/runs/34314724966/job/102348442267	
build	pass	43s	https://github.com/ccxt/ccxt/actions/runs/34314725028/job/102348443120	
build	pass	36s	https://github.com/ccxt/ccxt/actions/runs/34314725149/job/102348443493	
live-tests	pass	44s	https://github.com/ccxt/ccxt/actions/runs/34314725149/job/102348658699	
build	pass	55s	https://github.com/ccxt/ccxt/actions/runs/34314725039/job/102348443116	

=== confirm head lock STILL current (no new push) ===
debfd9bc408c793597209a3c4be1fa56ac6bd463	OPEN

=== DONE ===
